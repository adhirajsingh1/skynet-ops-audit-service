version: "3.9"

services:
  audit-service:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: skynet-audit-service
    ports:
      - "3000:3000"
    env_file:
      - .env.example
    restart: unless-stopped